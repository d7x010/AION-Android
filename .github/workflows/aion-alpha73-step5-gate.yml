name: AION α7.3 Step 5 Gate

on:
  push:
    branches: [main]
    paths:
      - "AION-ALPHA73-STEP5-SOURCE.zip"
      - ".github/workflows/aion-alpha73-step5-gate.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    name: Unit + Lint + APK
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: جلب المستودع
        uses: actions/checkout@v5

      - name: استخراج AION α7.3 dev6
        run: |
          set -euo pipefail
          test -f AION-ALPHA73-STEP5-SOURCE.zip
          rm -rf .aion-alpha73-step5
          mkdir .aion-alpha73-step5
          unzip -q AION-ALPHA73-STEP5-SOURCE.zip -d .aion-alpha73-step5
          test -f .aion-alpha73-step5/settings.gradle.kts
          test -f .aion-alpha73-step5/app/build.gradle.kts
          grep -q 'versionCode = 20' .aion-alpha73-step5/app/build.gradle.kts
          grep -q 'versionName = "2.0.0-alpha7.3-dev6"' .aion-alpha73-step5/app/build.gradle.kts

      - name: إعداد Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: إعداد Android SDK
        uses: android-actions/setup-android@v3

      - name: تثبيت Android 16 وأدوات البناء
        shell: bash
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: إعداد Gradle 9.6.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.6.1"

      - name: Worker regression + STEP5 voice tests
        working-directory: .aion-alpha73-step5
        run: node cloudflare/aion-core/test-worker.mjs

      - name: Android Unit + Lint + assembleDebug
        working-directory: .aion-alpha73-step5
        run: gradle :app:testDebugUnitTest :app:lintDebug :app:assembleDebug --stacktrace --no-daemon

      - name: التحقق من APK
        shell: bash
        run: |
          set -euo pipefail
          APK=.aion-alpha73-step5/app/build/outputs/apk/debug/app-debug.apk
          test -f "$APK"
          "$ANDROID_HOME/build-tools/36.0.0/apksigner" verify --verbose --print-certs "$APK"
          cp "$APK" AION-alpha73-step5-dev6.apk
          sha256sum AION-alpha73-step5-dev6.apk | tee AION-alpha73-step5-dev6.sha256

      - name: رفع APK
        uses: actions/upload-artifact@v4
        with:
          name: AION-alpha73-step5-dev6-APK
          path: |
            AION-alpha73-step5-dev6.apk
            AION-alpha73-step5-dev6.sha256
          if-no-files-found: error
          retention-days: 30
